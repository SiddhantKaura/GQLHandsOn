type Query {
  tracks: [Track!]!
}

type Track {
  id: String!
  title: String!
  thumbnail: String
  length: Int
  modulesCount: Int
  author: Author!
}

type Author {
  id: String!
  name: String!
  photo: String
}
